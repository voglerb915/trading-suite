// frontend/apps/dashboard/js/lists/renderActiveOrders.js

export function renderActiveOrders(state, container) {
    container.innerHTML = `
        <div class="info-box">
            <p>Active Orders werden später integriert.</p>
        </div>
    `;
}
