// frontend/apps/dashboard/js/lists/renderSignalsList.js

export function renderSignalsList(state, container) {
    container.innerHTML = `
        <div class="info-box">
            <p>Signals werden später integriert.</p>
        </div>
    `;
}
